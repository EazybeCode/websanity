import { SalesGrowthLpClient } from '@/components/lp/SalesGrowthLpClient'

/** Portuguese (BR) paid LP — exact translation of /lp/whastapp-sales-growth. */
export default function Page() {
  return <SalesGrowthLpClient locale="br" />
}
