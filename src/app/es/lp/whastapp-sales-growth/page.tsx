import { SalesGrowthLpClient } from '@/components/lp/SalesGrowthLpClient'

/** Spanish paid LP — exact translation of /lp/whastapp-sales-growth. */
export default function Page() {
  return <SalesGrowthLpClient locale="es" />
}
