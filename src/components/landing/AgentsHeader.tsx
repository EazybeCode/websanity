export function AgentsHeader() {
  return (
    <section className="agents-header" id="agents">
      <div className="container">
        <div className="sec-head centered reveal">
          <span className="sec-tag">Meet The Agents</span>
          <h2>Five <em>WhatsApp AI Agents</em> For Every Stage Of Your Pipeline</h2>
          <p style={{ maxWidth: 720, width: '100%', textAlign: 'center', hyphens: 'auto' }}>
            Each agent owns a different stage of the funnel, from first inbound to renewal. They share the same context engine, so a deal that starts in qualification never loses its history when it moves to close.
          </p>
        </div>
      </div>
    </section>
  )
}
